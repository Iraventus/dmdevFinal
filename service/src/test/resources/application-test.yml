spring:
  datasource:
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        current_session_context_class: thread
        show_sql: true
        format_sql: true
        hbm2ddl.auto: create
        connection:
          pool_size: 12